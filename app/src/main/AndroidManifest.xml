<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.foodapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodApp">
        <activity android:name=".ui.CartActivity"></activity>
        <activity android:name=".ui_drawer.SecurityActivity" />
        <activity android:name=".ui_drawer.PrivacyActivity" />
        <activity android:name=".ui_drawer.OfferPromoActivity" />
        <activity android:name=".ui_drawer.OrdersActivity" />
        <activity android:name=".ui_drawer.ProfileDrawerActivity" />
        <activity android:name=".bottom_navigation_view.ProfileBottomActivity" />
        <activity android:name=".ui.ItemViewInfoActivity" />
        <activity android:name=".bottom_navigation_view.HistoryActivity" />
        <activity android:name=".registration.RegistrationActivity" />
        <activity android:name=".registration.SplashActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.FoodApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>